/**
 * Discount Automation Service
 * Fully disabled per system requirement (No automated coupon generation/sending).
 */
async function runDiscountAutomationCheck() {
  // Automated coupon sending completely disabled
  return;
}

function startDiscountScheduler() {
  // Automated coupon scheduler completely disabled
  return;
}

module.exports = {
  runDiscountAutomationCheck,
  startDiscountScheduler
};
